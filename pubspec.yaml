name: trade_mate
description: "A new Flutter project."

publish_to: 'none' # Remove this if you plan to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # 🔥 Firebase core packages - UPDATED TO COMPATIBLE VERSIONS
  firebase_core: ^2.24.0
  firebase_auth: ^4.17.3
  google_sign_in: ^6.1.5
  cloud_firestore: ^4.13.3
  firebase_messaging: ^14.6.3
  firebase_storage: ^11.5.3
  open_filex: ^4.3.2
  path_provider: ^2.1.3
  syncfusion_flutter_xlsio: ^25.1.35
  excel: ^2.0.1
  
  # Other dependencies
  provider: ^6.0.5
  shared_preferences: ^2.2.2
  image_picker: ^1.0.4

  # UI
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  pdf: ^3.11.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # ✅ All your images and icons
  assets:
    - lib/assets/images/
    - lib/assets/icons/